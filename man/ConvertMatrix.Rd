% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Step1_MatrixConversion.R
\name{ConvertMatrix}
\alias{ConvertMatrix}
\title{Convert a sparse Matrix Market file into a dense data.table}
\usage{
ConvertMatrix(mtx_path, features_path, barcodes_path)
}
\arguments{
\item{mtx_path}{Character. Path to the Matrix Market file (.mtx.gz).}

\item{features_path}{Character. Path to the features file (e.g., features.tsv.gz).}

\item{barcodes_path}{Character. Path to the barcodes file (e.g., barcodes.tsv.gz).}
}
\value{
A `data.table` with rows as features (genes or peaks),
and columns as barcodes (cells), including a column `"row.names"` for feature IDs.
}
\description{
This function performs Step 1 of the exam:
It loads a sparse expression matrix from a `.mtx.gz` file,
along with corresponding features and barcodes, and converts
everything into a dense data.table suitable for downstream analysis.
}
\examples{
\dontrun{
# Example usage:
expr_dt <- ConvertMatrix(
  mtx_path = "data/expression/matrix.mtx.gz",
  features_path = "data/expression/features.tsv.gz",
  barcodes_path = "data/expression/barcodes.tsv.gz"
)
head(expr_dt)
}
}
